---
name: job-application-autofill
description: 读取完整网申 JSON，使用宿主浏览器勘察真实表单，由独立 agent 核验页面规则后批量填写，再由另一 agent 核验内容并按模块保存；沿用已有授权。DOM 优先。
---

# 网申回填

目标是尽快交付大部分已填好且状态清楚的申请。计划通过、输入返回、页面匹配、网站保存是不同结果；只根据真实工具返回和网页观察报告进度。

## 授权与资料

- 沿用当前申请已有的填写、暂存授权。用户已经要求使用现有方法填写时，不再要求回复“开始”或重复确认资料传输。本地计划、脚本、预检链接是进度信息，不是新审批关卡。新声明、缺失事实、最终提交分别按实际范围处理。
- 资料入口优先使用用户明确提供的 JSON 路径，其次读取环境变量 `JOB_APPLICATION_PROFILE` 指定的文件，最后检查当前工作目录的 `private/autofill-profile.json`。未找到时询问资料位置；不搜索无关目录，不使用公开示例替代真人资料。读取后任务内复用，文件更新或用户更正时定点重读。格式与空值语义见 [资料格式](references/profile-format.md)。
- 依据顺序：本轮用户更正 → JSON 已确认字段与策略 → JSON 有来源正文 → 对真正缺失、冲突、过时的信息查用户配置的履历库索引及对应条目（可选）。不能把只确认过某项推断为“只有该项”；不要重新润色已有可直接使用的正文。
- 按 `record_id` 盘点教育、实习、项目、研究、竞赛、奖项、校园和技能，遵守 `autofill_policy/presentation_policy`。实习、项目、论文各自归类；同一成果可以关联，但不能因类别混淆重复新增。竞赛和获奖要关联同一事件。
- 采用合适长度的 `description/responsibilities/achievements/texts/quick_fill`；汇总与逐条资料冲突时以当前记录为准。保留作者身份、真实研究状态、日期精度、排名范围和指标归属。
- 空值不等于“无/否”。结合 `collection_status/field_metadata` 区分未提供、明确无、不适用、不披露和待确认。先查正文再用 `missing_values` 判断缺项；历史公司的个人答案不自动用于新公司。
- 证件号码由用户本人输入，读取页面前排除这些值，不进入截图、日志或文件。年月不补造某日，不猜学位、证书编号、薪资或声明事实。真正缺失的问题集中交接，不阻塞其他确定字段。
- 用户要求全部补齐时，完整检查相关 JSON 栏目后再判断缺项。不能因一个字段要手动，就把整条记录或整个栏目直接排除。

## 页面和准备

采用“主 agent 勘察 → 独立规则核验 → 批量填写 → 另一 agent 内容核验 → 集中修正与保存”。开始操作前读 [独立核验流程](references/independent-checks.md)。两个核验者使用独立上下文；不得由主 agent 代写通过结果。规则核验先自行观察真实页面，再对照主 agent 的结论。共享浏览器串行交接。

复用用户指定浏览器和申请页。先核对目标页面、字段标签、控件类型、现值、必填项和记录边界。已展开的空白表单可以直接填写，不再次新增；尚未展开不等于没有栏目。编辑中不能为了盘点切走并丢失草稿。

连接恢复只在确有句柄故障时使用 [连接检查器](references/connection-contract.md)，不把普通字段定位失败当成连接失败。保持两次连接预算，优先采用当前工具文档中适用的路径；不抢占其他任务，不用新页面替代当前草稿。

按 [岗位筛选](references/jd-selection.md) 记录每条经历的去向。JD 不可得就明确标注，按用户已有的网申/通用履历范围继续确定内容，不能因此反复索要相同范围的授权。

准备 `alignment-plan.json` 与不含证件值的 `before.json` 即可开始本地检查；完整盘点按申请保留，各小批次只更新变化部分。交接清单是执行结果，不要求开工前先写一份。是否需要 `batch.js` 由实际执行方式决定。

## 选择真实执行方式

先读 [执行与恢复](references/execution-contract.md)。

1. **direct-tool（默认）**：通过当前已经可用的浏览器工具执行实际代码/动作，保存实际调用及返回证据。不要求本地脚本加载，也不要为了满足格式创建只有注释或目标清单的 `batch.js`。
2. **adapter**：仅当当前主机确实支持加载本地代码、实际浏览器适配器和可靠状态落盘时，使用 `scripts/bounded_batch.js`。没有实际实现就退回 direct-tool，不声称“脚本已执行”。不得安装连接服务或绕过工具权限来满足此模式。

DOM/语义控件优先，其次获准的原生输入，最后短而明确的视觉操作。此顺序不要求每个失败字段轮换所有通道。普通文本整段输入，多个独立字段在短批次内顺序执行；不逐字符或逐字段反复推理。

`preflight.py --run <目录>` 分开输出：
- `planReady`：本地结构、来源声明、覆盖关系和旧值映射通过。
- `ready`：本批次还有明确的执行方式及近期真实定位探测记录，可以开始相应操作；不证明代码已运行、事实真实或网站已保存。
- `automaticCount/dispatchIds`：整份计划的自动目标和这次实际批次。零自动目标只能称为交接计划。

定位探测只检查本批次的页面、唯一字段、真实现值和控件类型；失败时修定位或跳过，不重做整份履历计划。探测过期只更新只读证据，不重新询问已获授权。工具调用编号和结果必须取自实际返回，不可自造“成功证明”。

## 执行、恢复与保存

- 同一申请保留原始开始时间、授权依据、已完成项和未决操作。30 分钟总预算包含准备、连接、填写、回读、保存；小批次、恢复和重新读取不清零。普通操作上限 120 秒，保存上限 180 秒，串行动作共享剩余时间。工具外层须留返回余量，不能把配置数字当作底层已生效。
- 独立规则核验完成后，在已确认范围内批量填写。只在首次使用未验证的输入方法时做最小操作探测与回读；不逐字段重复内容核验。必要的动态控件观察与未知写入恢复不省略。新规则只复核受影响部分；难控件不长时间排错，不阻塞独立项。
- 只读错误：明确调用已结束且未写入时，可以修正一次或跳过，继续独立项。未决远程调用先暂停等待/核对；不能凭“读取”假定调用已经结束。
- 写入错误：区分确定未生效、部分新增、写入未知和回读不匹配。未知写入先持久记录，再只读核对；无法判定时交接，不能原样重放。
- 恢复时先看真实网页。已匹配项跳过；用户修改保留并标冲突；依赖的记录已存在且核对一致时直接使用。部分新增只能通过此前记录的稳定记录标识找回同一条空白记录并补名称，不再点“添加”。找不到可靠标识就交接。
- 每个模块按站点实际按钮保存：独立内容核验通过并修正 → 核对现场必填条件 → 在已有授权内保存 → 只读验证成功。缺必填项只阻塞该模块保存，其他独立模块继续；不能为了满足“一次保存”把各栏目留成草稿。
- 保存未知时保留页面，只读核对，不重复点击或刷新。已确认保存的模块不重复保存；保存校验明确失败后，仅在缺项已经变化并核对通过时再尝试。
- 正式提交仍单独受用户授权约束；执行器不提供自动提交动作。不要把“预览并提交”当暂存。
- 旧版本执行状态不能直接冒充新状态重跑。保留旧记录，先核对当前页面和新增记录，再迁移已完成项；尚未解决的未知动作继续保留，不能靠新目录消除。

## 核验与交接

独立内容核验须从原始资料盘点应处理记录，再检查真实页面，不能只验证填写者选中的字段。没有独立核验结果不能报告验收通过；没有保存证据不能称已保存。`preflight.py`、`verify_form.py` 均不检查核验 agent 身份，也不替代规则或内容核验；当前未接入宿主强制拦截。

`verify_form.py` 只比较提供的快照。请求 `--evidence reloaded` 必须附真实保存、重开及读取记录，缺失则降为 `page`；提供记录也不等于离线工具能独立证明网站事实。格式见 [核验格式](references/plan-schema.md)。

报告实际填写项、保存成功模块、未保存草稿和阻塞原因，并交代完整申请还有哪些确定内容未处理。禁止用缩小后的“3/3”代替整份完成度。手动项列“栏目/记录 → 字段 → 值或缺失事实 → 未尝试/缺资料/失败/保存丢失”，主动省略单列理由。保留当前申请页供用户接手。

`zhiye.com` 现场确认为 Phoenix 时可参考 [站点经验](references/zhiye.md)，不能把旧租户方法当当前站点已经验证。所有检查只约束经过它们的路径，无法阻止模型绕过脚本直接调用工具；不要承诺工具层强制隔离。
